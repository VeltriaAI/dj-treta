# Python DJ Engine (Obsolete)

These files were the original pure-Python DJ engine built in the first session.
Replaced by the Mixxx fork (VeltriaAI/mixxx, branch: feature/http-api) which provides
professional-grade beat matching, time-stretching, waveforms, and effects.

The HTTP API is now built into Mixxx's C++ codebase at:
  ~/workspace/mixxx-treta/src/api/apiserver.cpp

These files are kept for reference only.
